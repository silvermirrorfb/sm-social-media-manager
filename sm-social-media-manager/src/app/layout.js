export const metadata = {
  title: 'Silver Mirror Social Media Manager',
  description: 'AI-powered Instagram DM and comment management for Silver Mirror Facial Bar',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
